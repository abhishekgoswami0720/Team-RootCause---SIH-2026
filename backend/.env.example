# Postgres connection string for the cloud/primary DB
DATABASE_URL=postgresql://user:password@host:5432/mandiq

# Optional local fallback DB (used automatically if DATABASE_URL is unreachable)
LOCAL_DB_URL=postgresql://user:password@localhost:5432/mandiq

# "auto" (default) | "local" | "cloud" — force which DB to use
DB_MODE=auto
